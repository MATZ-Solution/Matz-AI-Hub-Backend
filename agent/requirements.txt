# Core agent
langgraph==0.5.4
langchain-core==0.3.39

# LLM
groq>=0.20.0,<1

# Vector store & embeddings
qdrant-client>=1.13.0,<2
sentence-transformers>=3.3.0,<4
transformers>=4.50.0,<5
torch==2.13.0

# Database / auth
supabase>=2.15.0,<3

# HTTP client — pinned to avoid the proxies= kwarg breakage in httpx 0.28+
httpx>=0.27.0,<0.28
httpcore>=1.0.0,<2

# Document extraction
pypdf>=5.0.0,<6
python-docx>=1.1.0,<2
python-pptx>=1.0.0,<1.1
openpyxl>=3.1.0,<4
# docling>=1.18.0,<2  # optional heavy OCR fallback — disabled for Python 3.14 (deepsearch-glm unavailable)

# Utilities
python-dotenv==1.0.1

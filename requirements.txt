# Web framework — these were missing: the app ran locally only because they
# happened to be installed globally, so a clean install (Docker, CI, a new
# teammate's venv) would fail at `import fastapi`.
# python-multipart is required by FastAPI for UploadFile / form data.
fastapi>=0.115.0,<1
uvicorn[standard]>=0.30.0,<1
python-multipart>=0.0.9,<1

# Core agent
langgraph==0.5.4
langchain-core==0.3.39

# LLM
groq>=0.20.0,<1

# Vector store & embeddings
qdrant-client>=1.13.0,<2
sentence-transformers>=3.3.0,<4
transformers>=4.50.0,<5
torch==2.13.0

# Database / auth
supabase>=2.15.0,<3

# HTTP client — pinned to avoid the proxies= kwarg breakage in httpx 0.28+
httpx>=0.27.0,<0.28
httpcore>=1.0.0,<2

# Document extraction
pypdf>=5.0.0,<6
python-docx>=1.1.0,<2
python-pptx>=1.0.0,<1.1
openpyxl>=3.1.0,<4
# Renders scanned PDF pages to images for the vision model (not an OCR engine)
pymupdf>=1.24.0,<2
# docling>=1.18.0,<2  # optional heavy fallback for complex layouts/tables

# Utilities
python-dotenv==1.0.1
# Spell correction for English queries (agents/.../utils.correct_query).
# TextBlob's .correct() uses its own bundled spelling model, so no NLTK
# corpus download is needed at build or runtime.
textblob>=0.18.0,<1
